AAAAAAAAAAA
{{define "cashflow"}}
{{template "header" .}}
<div class="container">
    <div class="container">
        {{if .Error}}
            <div class="text-danger">{{.Error}}</div>
        {{end}}
    </div>

    <h1>Accounts</h1>
    <ul>
        {{range .Accounts}}
        <li>{{.Name}}</li>
        {{end}}
    </ul>
</div>
{{template "footer" .}}
{{end}}